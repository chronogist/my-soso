services:
  redis:
    image: redis:7-alpine
    container_name: my-soso-redis
    command:
      ['redis-server', '--save', '', '--appendonly', 'no', '--maxmemory-policy', 'noeviction']
    ports:
      - '6379:6379'
    restart: unless-stopped
