app = "my-soso"
primary_region = "ord"

[build]
  dockerfile = "Dockerfile.worker"

[env]
  NODE_ENV = "production"
  LOG_LEVEL = "info"

[processes]
  app = "pnpm --filter @my-soso/worker start"

[deploy]
  strategy = "immediate"
